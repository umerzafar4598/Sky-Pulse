<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Animated Clouds */
        .clouds {
            background: url("https://i.ibb.co/5LYshxp/clouds.png");
            animation: moveClouds 60s linear infinite;
        }

        @keyframes moveClouds {
            from {
                background-position: 0 0;
            }

            to {
                background-position: 2000px 0;
            }
        }
    </style>
</head>

<body class="bg-gradient-to-br from-blue-200 to-blue-500 min-h-screen flex justify-center items-center overflow-hidden">

    <!-- Animated Clouds -->
    <div class="absolute top-0 left-0 w-full h-full opacity-30 clouds"></div>

    <div
        class="relative bg-white/70 backdrop-blur-md border border-white/40 shadow-xl rounded-2xl p-10 w-[90%] max-w-md">

        <h1 class="text-4xl font-extrabold text-center text-blue-900 mb-6">
            Weather Checker
        </h1>

        <form action="/check-weather" method="POST" class="flex flex-col gap-4">
            <input type="text" name="city" placeholder="Enter city name..." required
                class="px-4 py-3 rounded-xl border border-blue-300 focus:ring-2 focus:ring-blue-500 outline-none text-gray-700 shadow-sm" />

            <button
                class="bg-blue-600 hover:bg-blue-700 text-white py-3 rounded-xl font-semibold text-lg shadow-md transition">
                Check Weather
            </button>
        </form>

    </div>
</body>

</html>