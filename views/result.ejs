<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Weather Result</title>
  <link rel="stylesheet" href="/styles/main.css">
</head>

<body>

  <button class="toggle-theme" onclick="toggleTheme()">🌙</button>

  <div class="card">
    <h1>
      <%= name %>, <%= country %>
    </h1>
    <p>
      <%= region %> • Local Time: <%= time %>
    </p>

    <img src="https:<%= icon %>" alt="Weather Icon" style="width: 100px; margin: 15px auto; display:block;">

    <div class="temp">
      <%= temp %>°C
    </div>

    <div class="advice-box">
      <%= weatherBasedAdvice %>
    </div>

    <h2 class="text-xl font-semibold text-gray-700 mb-3">7-Day Forecast</h2>
    <div class="grid grid-cols-1 gap-3">
      <% forecast.forEach(day=> { %>
        <div class="flex items-center justify-between bg-white/70 shadow-sm rounded-xl p-3 border border-white/40">
          <span class="font-medium text-gray-800">
            <%= day.date %>
          </span>
          <img src="https:<%= day.icon %>" alt="icon" class="w-10">
          <span class="text-gray-600">
            <%= day.condition %>
          </span>
          <span class="text-blue-700 font-semibold">
            <%= day.max %>° / <%= day.min %>°
          </span>
        </div>
        <% }); %>
    </div>
    
    <a href="/" class="back-link">← Check Another City</a>
  </div>

  <script>
    function toggleTheme() {
      document.body.classList.toggle("dark");
      localStorage.setItem("theme", document.body.classList.contains("dark") ? "dark" : "light");
    }
    if (localStorage.getItem("theme") === "dark") document.body.classList.add("dark");
  </script>

</body>


</html>