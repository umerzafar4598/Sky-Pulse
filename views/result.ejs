<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Weather Result</title>

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-blue-100 to-blue-300 min-h-screen flex items-center justify-center">

  <div class="bg-white/70 backdrop-blur-md shadow-xl rounded-2xl p-8 w-[90%] max-w-md text-center border border-white/40">

    <!-- Location Heading -->
    <h1 class="text-3xl font-bold text-gray-800 mb-2">
      <%= name %>, <%= country %>
    </h1>
    <p class="text-gray-600 text-sm mb-4">
      <%= region %> • Local Time: <%= time %>
    </p>

    <!-- Weather Icon -->
    <div class="flex justify-center mb-4">
      <img src="https:<%= icon %>" alt="Weather Icon" class="w-28 h-28 drop-shadow-md">
    </div>

    <!-- Temperature -->
    <div class="text-5xl font-semibold text-blue-700 mb-3">
      <%= temp %>°C
    </div>

    <!-- Advice Card -->
    <div class="bg-blue-600 text-white py-3 px-4 rounded-xl shadow-md mt-4">
      <p class="text-lg font-medium">
        <%= weatherBasedAdvice %>
      </p>
    </div>

    <!-- Button to Go Back -->
    <a href="/" class="inline-block mt-6 text-blue-700 font-semibold hover:underline">
      ← Check Another City
    </a>
  </div>

</body>
</html>
